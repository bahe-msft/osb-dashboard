// Package dashboard provides an embeddable OpenSandbox web dashboard.
//
// Dashboard templates and static web assets are embedded in the package, so a
// consuming application can serve the complete UI from Dashboard.Handler
// without deploying additional files.
package dashboard
